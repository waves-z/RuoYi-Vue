<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.bbs.mapper.BbsLikeMapper">
    
    <resultMap type="BbsLike" id="BbsLikeResult">
        <result property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="selectBbsLikeVo">
        select id, post_id, user_id, create_time from bbs_like
    </sql>

    <select id="selectBbsLikeList" parameterType="BbsLike" resultMap="BbsLikeResult">
        <include refid="selectBbsLikeVo"/>
        <where>  
            <if test="postId != null "> and post_id = #{postId}</if>
            <if test="userId != null "> and user_id = #{userId}</if>
        </where>
    </select>
    
    <select id="selectBbsLikeById" parameterType="Long" resultMap="BbsLikeResult">
        <include refid="selectBbsLikeVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertBbsLike" parameterType="BbsLike" useGeneratedKeys="true" keyProperty="id">
        insert into bbs_like
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="postId != null">post_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="postId != null">#{postId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateBbsLike" parameterType="BbsLike">
        update bbs_like
        <trim prefix="SET" suffixOverrides=",">
            <if test="postId != null">post_id = #{postId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteBbsLikeById" parameterType="Long">
        delete from bbs_like where id = #{id}
    </delete>

    <delete id="deleteBbsLikeByIds" parameterType="String">
        delete from bbs_like where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="checkLike" resultType="int">
        select count(1) from bbs_like where post_id = #{postId} and user_id = #{userId}
    </select>

    <delete id="deleteLike">
        delete from bbs_like where post_id = #{postId} and user_id = #{userId}
    </delete>

</mapper> 